[package]
name = "dwara-console"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "dwara web console v3 (live charts, CRUD flows, AI ops) -- a static SPA served from the mTLS admin listener"
publish = false

[dependencies]
