{{- if .Values.gatewayClass.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gatewayClass.name }}
  labels:
    {{- include "dwara.labels" . | nindent 4 }}
spec:
  controller: {{ .Values.gatewayClass.controller }}
{{- end -}}
