# Packaging notes (DW-026)

## Artifacts

| Artifact | Path | Notes |
|---|---|---|
| scratch image | `Dockerfile.scratch` (repo root) | static musl binary on `FROM scratch` |
| distroless image | `Dockerfile.distroless` (repo root) | same binary on `gcr.io/distroless/cc-debian12:nonroot` |
| quickstart | `quickstart/` | compose file + demo upstream + self-signed TLS |
| systemd unit | `packaging/systemd/dwara.service` | hardened unit + install notes in the header comment |
| config reference | `config-reference.json` (repo root) | generated by `dwara-cli schema`; CI checks freshness |

## Binary size

The <25 MB bar is met by the release profile in the workspace
`Cargo.toml`: `lto = true` (fat LTO, since DW-001) plus `strip = true`
(DW-026). `panic` deliberately stays `unwind` — `panic = "abort"` would
shrink further but changes error semantics. `opt-level = z` was measured
as unnecessary once stripping is on (and costs codegen speed); revisit
only if the bar regresses. The static musl binary is larger than a gnu
build of the same code (musl's static libc + bundled SQLite + aws-lc-rs
C code all land in the file), but it is the price of a `FROM scratch`
runtime: the IMAGE ends up smaller than any gnu-based one.

Measured (aarch64 musl, DW-026 local verification): `dwara` binary
11,564,624 bytes (~11.0 MB); `dwara:scratch` image 17.6 MB; the
distroless variant is 65.2 MB (its Debian base dominates). The release
workflow re-checks the bar per arch (build fails if the stripped musl
binary exceeds 25 MB); amd64 is typically a few MB larger than aarch64.

## musl vs gnu

- The builder is `rust:1.94-alpine` with `musl-dev cmake make perl` —
  aws-lc-rs (rustls provider) compiles C via cmake/perl; none of it
  reaches the final image.
- Upstream TLS verification uses webpki-roots (Mozilla root set)
  compiled into the binary, so images ship no CA bundle. To trust a
  private CA for upstreams, that support must come from config (future
  work), not the filesystem.
- gnu builds are not shipped: dynamically linked, need a glibc runtime
  layer, and give no advantage for this deployment model.

## Which image when

- `Dockerfile.scratch`: absolute minimum; requires the orchestrator to
  inject an unprivileged user (`--user`, compose `user:`, k8s
  `runAsNonRoot`) because scratch has no user database.
- `Dockerfile.distroless`: baked-in UID 65532 (`nonroot`) plus tzdata
  and a CA bundle; use when you cannot inject a user or want the
  runtime to be self-describing.

## Release workflow

`.github/workflows/release-artifacts.yml` runs ONLY on `v*` tag push
(per the project's standing version-tags-only image publishing policy):
cross-builds amd64+arm64 musl binaries, checks the 25 MB bar, builds
multi-arch images, and pushes them to GHCR. Nothing builds on PRs or
pushes to main.
